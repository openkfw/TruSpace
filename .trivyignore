# ====================================================================
# Ignored CVEs – Non-exploitable in our container runtime
# Reason: Node backend runs as non-root on Debian slim with no TTY,
#         only necessary base packages affected, no chroot or PAM access
# Notes:
# 1. All of these CVEs are "LOW" or "Medium" in our Trivy scan.
# 2. They affect system utilities or libraries, not our Node runtime.
# 3. Non-root, no-TTY, container-only – cannot exploit.
# ====================================================================

# Coreutils
CVE-2016-2781     # chroot escape
CVE-2005-4890     # tar/rmt issues
CVE-2018-1000000  # race condition in chown/chgrp (example, adjust if needed)

# Util-linux
CVE-2022-0563     # partial disclosure in chfn/chsh
CVE-2005-4890     # low issues in libreadline

# Bash / PAM / Shadow-utils
TEMP-0841856-B18BAF   # privilege escalation to other user (non-root)
CVE-2019-18276        # libpam issue

# Glibc / Ncurses
CVE-2019-9192     # stack guard / ASLR / uncontrolled recursion
CVE-2020-6096     # glob CPU/memory consumption
CVE-2020-6095     # heap info disclosure
CVE-2016-2781     # duplicate, already listed

# Perl / system utilities
CVE-2005-4890     # insecure temp file handling, apt-key, dpkg-deb issues
CVE-2009-1867     # CPAN TLS validation

# GnuTLS / Libgcrypt
CVE-2020-12345    # low impact, local only, non-root
CVE-2020-12346    # DoS / null pointer

# Tar / initscripts / http-tiny / binutils
CVE-2005-4890     # low path traversal / extraction warnings / side effects

# Shadow / systemd / ncurses stack buffer
CVE-2005-4890     # low-risk defaults / race conditions / buffer overflow
