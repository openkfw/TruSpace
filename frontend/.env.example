NEXT_PUBLIC_API_URL=http://localhost:8000/api

# If true (default), passwords set during registration and password reset must meet strict rules.
# Set to false to allow any password (useful for local development and testing).
REQUIRE_STRICT_PASSWORDS=true
